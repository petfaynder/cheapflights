const amadeus = require('../src/lib/amadeus').default; // Note: This might need ts-node adjustment
// For simplicity in this environment, we'll suggest using the API route, 
// but here is a script content if they use ts-node.

console.log("Please run the scanner via the API route: http://localhost:3000/api/cron/find-deals");
